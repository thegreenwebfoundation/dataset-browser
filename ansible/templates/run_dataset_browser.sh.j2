#! /usr/bin/bash

# {{ ansible_managed }}
# Last run: {{ template_run_date }}

# calling `exec` here means that systemd sends a KILL command just
# allowing for a graceful shutdown or reboot
#
# Using gunicorn for proper access logging and production readiness.
# --forwarded-allow-ips handles X-Forwarded-For from Caddy reverse proxy
exec .venv/bin/datasette . -h 0.0.0.0 -p {{ app_port }}